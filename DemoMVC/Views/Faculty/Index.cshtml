@*@model IEnumerable<Sinhvien.Models.Faculty>

<h2>Danh sách khoa</h2>

<a asp-action="Create" class="btn btn-primary">Thêm khoa</a>

<table class="table">
    <thead>
        <tr>
            <th>Mã khoa</th>
            <th>Tên khoa</th>
            <th>Thao tác</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.FacultyId</td>
                <td>@item.FacultyName</td>
                <td>
                    <a asp-action="Details" asp-route-id="@item.FacultyId">Chi tiết</a> |
                    <a asp-action="Edit" asp-route-id="@item.FacultyId">Sửa</a> |
                    <a asp-action="Delete" asp-route-id="@item.FacultyId">Xóa</a>
                </td>
            </tr>
        }
    </tbody>
</table>*@
@model IEnumerable<DemoMVC.Models.Entities.Faculty>

<h2>Danh sách khoa</h2>

<a asp-action="Create">Thêm khoa</a>

<table class="table">
    <thead>
        <tr>
            <th>Mã khoa</th>
            <th>Tên khoa</th>
            <th>Thao tác</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.FacultyId</td>
                <td>@item.FacultyName</td>
                <td>
                    <a asp-action="Details" asp-route-id="@item.FacultyId">Details</a> |
                    <a asp-action="Edit" asp-route-id="@item.FacultyId">Edit</a> |
                    <a asp-action="Delete" asp-route-id="@item.FacultyId">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>